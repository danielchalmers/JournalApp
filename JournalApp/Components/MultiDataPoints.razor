@namespace JournalApp
@typeparam T where T : DataPoint

@foreach (var dataPoint in DataPoints.OrderBy(x => x.Id))
{
	<DataPointView DataPoint="dataPoint" />
}

@code {
	[Parameter]
	public ICollection<T> DataPoints { get; set; }
}