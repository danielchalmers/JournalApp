@namespace JournalApp

<div class="switcher">
	<MudIconButton Icon="@Icons.Material.Filled.ArrowBackIos" Title="Previous year" OnClick="() => SelectedYear--" />

	<div class="switcher-header">
		<MudButton OnClick="() => SelectedYear = DateTime.Now.Year">
			<MudText Typo="Typo.h5">@SelectedYear</MudText>
		</MudButton>
	</div>

	<MudIconButton Icon="@Icons.Material.Filled.ArrowForwardIos" Title="Next year" OnClick="() => SelectedYear++" Disabled="@(SelectedYear >= DateTime.Now.Year)" />
</div>

@code {
	int _selectedYear;

	[Parameter]
	public int SelectedYear
	{
		get => _selectedYear;
		set
		{
			_selectedYear = value;
			SelectedYearChanged.InvokeAsync(value);
		}
	}

	[Parameter]
	public EventCallback<int> SelectedYearChanged { get; set; }
}
